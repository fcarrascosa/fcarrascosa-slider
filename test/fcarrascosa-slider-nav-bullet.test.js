import {
  html,
  fixture,
  expect,
} from '@open-wc/testing';

describe('<fcarrascosa-slider-nav-bullet>', () => {
  describe('common tests', () => {
    let element;

    beforeEach(async () => {
      element = await fixture(html`<fcarrascosa-slider-nav-bullet></fcarrascosa-slider-nav-bullet>`);
    });

  });
});
